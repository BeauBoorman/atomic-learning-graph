Atomic Learning Graph — OpenAI Build Week (submission 2026-07-21 17:00 PT):

Note: the core is BUILT and green — invariants, atomizer, getPath, UI, renderings all ship.
Everything below is SHIPPING work. Do not re-add build tasks from git history; check `git log`
and `pnpm gate` before believing any status written here. (This file lied for 4 days: it
@next'd invariants.ts and named Next.js/React Flow/Vercel, none of which is what got built.)

Doing:
	- adversarial code audit + creative gap pass (what's missing / what makes it better) @next @priority(1)

To Do:
	- secret + history audit -> SAFE/NOT-SAFE verdict artifact; rules on the 23 API response IDs in data/graph.run.json @priority(1)
	- push the 3 unpushed commits to forgejo @priority(2)
	- VERIFY or CUT the Khan Academy / Khanmigo figures in README.md before publishing @me @priority(1)
	- README demo link + 6s GIF (goal -> lesson -> footnote -> source sheet -> highlight) @priority(2)
	- publish PUBLIC repo — IRREVERSIBLE, gated on the secret audit @me @priority(1)
	- record demo video (<3:00, YouTube) — spine: RED->GREEN (516137e -> codex/core-build) + course routing, silent network tab @me @priority(1)
	- capture Codex /feedback session ID from the core-build session @me @priority(1)

Done:
	- repo scaffold, TypeScript + vitest toolchain, red invariant suite @done(2026-07-13)
	- src/graph/invariants.ts green — 5 hard invariants + isSingleConcept advisory @done(2026-07-15)
	- OER -> concept-graph atomizer (src/atomization/atomize.ts -> data/graph.json) @done(2026-07-15)
	- prerequisite-edge inference @done(2026-07-15)
	- deterministic pathfinder walk over the prereq DAG (src/graph/path.ts) @done(2026-07-15)
	- UI shipped as Vite + React + Cytoscape (supersedes the Next.js/React Flow/dagre plan) @done(2026-07-16)
	- licence split — code MIT / content CC-BY-SA-4.0 @done(2026-07-16)
	- renderings pipeline + per-rendering citation gate @done(2026-07-16)
	- gate9 CSS network hole closed @done(2026-07-16)
	- GitHub Pages base path (supersedes "deploy to Vercel"; a live URL is NOT required) @done(2026-07-16)
	- purge stale state claims from AGENTS.md + ~/.codex/memories that told agents the build was red @done(2026-07-17)
