---
title: "Atomic Learning Graph"
aliases:
  - "Start Here"
tags:
  - "atomic-learning-graph"
---

# Atomic Learning Graph

This is a ready-to-use Obsidian course: open this folder as a vault, start with this note, and follow the linked concepts in order.

Follow the concepts below in prerequisite order. Every concept and lesson step carries a verbatim source receipt.

**Goal:** [[self-attention|Self-Attention]]

## Learning path

1. [[vectors|Vectors]] — A vector is a fixed-length array whose elements are scalars.

2. [[dot-product|Dot Product]] — The dot product of two vectors sums the products of elements at corresponding positions.

3. [[matrix-vector-product|Matrix–Vector Product]] — A matrix–vector product contains one dot product between the input vector and each row of the matrix.

4. [[softmax|Softmax]] — Softmax exponentiates scores and normalizes them into values that sum to one.

5. [[qkv|Queries, Keys, and Values]] — Attention can be framed using a query applied to a collection of key–value pairs.

6. [[attention-pooling|Attention Pooling]] — Attention pooling combines values linearly using weights determined by query–key compatibility.

7. [[self-attention|Self-Attention]] — Self-attention lets every token attend to every other token in the same sequence.

8. [[positional-encoding|Positional Encoding]] — Positional encodings provide token-order information to models as additional token-associated inputs.

9. [[softmax-ordering|Softmax Preserves Ordering]] — Softmax preserves score ordering, so the largest input score identifies the class with the highest output probability.

10. [[vector-norm|Vector Norm]] — A vector norm provides a scalar measure of the vector's magnitude.

## How to use this vault

Open a concept, read its lesson, and follow its prerequisite links when you need to step back. Source passages and attribution stay beside the claims they ground.
